from django.apps import AppConfig


class ContabilidadAuditConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "contabilidad_audit"
    verbose_name = "Auditoría contable"
