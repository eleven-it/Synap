from django.apps import AppConfig


class FacturaCompraCapturaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "factura_compra_captura"
    verbose_name = "Factura compra — captura y workflow"
