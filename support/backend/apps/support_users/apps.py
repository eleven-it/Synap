from django.apps import AppConfig


class SupportUsersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.support_users"
    verbose_name = "Usuarios de soporte"
