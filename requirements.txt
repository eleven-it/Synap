# Dependencias mínimas para módulo de Reportes
Django==4.2
djangorestframework
psycopg2-binary==2.9.9
gunicorn==21.2.0
whitenoise==6.9.0
dj-database-url==0.5.0
django-tailwind
requests
PyJWT==2.8.0
cryptography
python-decouple
mysqlclient==2.2.7  # Para conexión a MySQL de administraNET
pymssql>=2.2.0,<3  # Azure SQL BEST (migración MPR, solo lectura)
django-allauth
django-redis
django-crispy-forms==2.4
crispy-tailwind==1.0.3
Pillow==10.1.0
webauthn>=2.0,<3
six==1.16.0
django-widget-tweaks==1.5.0
openpyxl  # Para exportación de reportes a Excel
pyxlsb  # Lectura de planillas Monthly Reporting .xlsb (seed licenciatarios)
reportlab  # Para exportación de reportes a PDF
django-filter
PyYAML
django-cors-headers
defusedxml
bleach
pypdf>=4.0,<6
pytesseract>=0.3.10,<0.4
opencv-python-headless>=4.8,<5

paramiko>=3.4,<4

# FE AFIP / padrón (WSSrPadron A4–A5): SOAP; pyafipws se instala desde ./pyafipws local (Dockerfile) o pip -e ./pyafipws
pysimplesoap>=1.16.2,<2
future>=0.18.0,<1  # import chain pyafipws / pysimplesoap (No module named 'future')

# Dependencias eliminadas (no necesarias para Reportes básico):
# fastapi - No necesario
# uvicorn - No necesario
# firebase-admin - No necesario
celery>=5.3,<6  # tiendanube_administranet: sincronización en segundo plano (requiere broker en producción)
# django-celery-beat>=2.8.1 - No necesario
# mysql-connector-python - Redundante con mysqlclient
# geopy - No necesario
# openai>=1.10.0 - Solo para Reports AI
# crewai>=0.28.0 - Solo para Reports AI
# langchain>=0.1.0 - Solo para Reports AI
# langchain-openai>=0.0.5 - Solo para Reports AI