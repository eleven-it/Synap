from django.apps import AppConfig


class FacturaCompraPostingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "factura_compra_posting"
    verbose_name = "Factura compra — posting legacy"
