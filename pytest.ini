[pytest]
DJANGO_SETTINGS_MODULE = django_project.settings
python_files = test_*.py
addopts = -q --reuse-db
markers =
    integration: tests que requieren MySQL legacy u otros servicios externos
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
