# Scripts SQL MPR (documentación / manual)

Los scripts de esta carpeta son **referencia y ejecución manual** en desarrollo.

El DDL **operativo** que ejecuta la herramienta global y `manage.py apply_mpr_core_tables` está en **`mpr/sql/001_mpr_core_tables.sql`** (incluido en Staging y Produccion).
